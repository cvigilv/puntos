# Remap prefix to 
set -g prefix C-space
unbind C-b
bind C-space send-prefix

# Bindings
unbind r; bind r source-file ~/.tmux.conf	# Reload configuration

set pane-active-border-style "fg=#ffb52a"
set pane-border-style "fg#5f5f5f"
